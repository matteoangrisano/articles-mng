schema {
  query: Query
  mutation: Mutation
}

type Query {
  getArticle(id: Int): Article
}

type Mutation {
  postArticle(id: Int, title: String, author: String): Article
}

type Article {
  id: Int
  title: String
  author: String
}
