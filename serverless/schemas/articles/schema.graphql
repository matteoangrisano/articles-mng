schema {
  query: Query
  mutation: Mutation
}

type Query {
  getArticle(id: Int): Article
}

# type Mutation {
#   addDefaultGroups(tenant: String): String
# }

type Article {
  id: Int
  title: String
  author: String
}
